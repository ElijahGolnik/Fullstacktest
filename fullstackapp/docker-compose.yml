services:
  app:
    build: .
    ports:
      - "3000:3000"
    command: npm start
    develop:
      watch:
        - action: sync
          path: ./app
          target: /src/app
          initial_sync: true
          ignore:
            - node_modules/
        - action: rebuild
          path: package.json
